## How to access the Kubernetes cluster.

```
oob-mgmt-server$ ssh -i $HOME/.ssh/kargo-test.priv k8s1
```

The first node in the cluster ``k8s1`` has ``kubectl`` the admin cli
application for Kubernetes. Example

```
oob-mgmt-server$ ssh -i $HOME/.ssh/kargo-test k8s1
k8s1$ kubectl get node

```

