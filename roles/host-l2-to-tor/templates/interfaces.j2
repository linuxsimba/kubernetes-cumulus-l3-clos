auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp

auto eth1
iface eth1
  address {{ tor_facing_ip }}
  up ip route add {{ tor_agg_route }} via {{ tor_gw }}
  down ip route del {{ tor_agg_route }}  via {{ tor_gw }}

auto vagrant
iface vagrant
